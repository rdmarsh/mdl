- content_for(:title, t('environment').pluralize.humanize)

- content_for(:index_tab, environments_path)
- content_for(:new_tab, new_environment_path)

= render 'layouts/heading'
= render 'layouts/tabs'