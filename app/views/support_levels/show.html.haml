= render 'head'

= show_for @supportlevel do |f|
  = f.attribute :name
  = f.attribute :hours
  = f.attribute :days
  = f.attribute :exclusions
  = f.attribute :description
  = f.attribute :updated_at
  = link_to "Change", edit_support_level_path(@supportlevel), :class => "btn btn-primary"