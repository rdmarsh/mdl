- content_for(:title, t('announcement').pluralize.humanize)
- content_for(:description, t('announcement_description').humanize)

- content_for(:index_tab, announcements_path)
- content_for(:new_tab, new_announcement_path)

= render 'layouts/heading'
= render 'layouts/tabs'